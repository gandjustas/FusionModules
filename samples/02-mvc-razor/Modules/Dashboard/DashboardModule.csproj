<Project Sdk="Microsoft.NET.Sdk.Razor">

  <PropertyGroup>
    <!-- Clears the single-TFM default from samples/Directory.Build.props, which would win. -->
    <TargetFramework />
    <TargetFrameworks>net8.0;net10.0</TargetFrameworks>
    <!--
      Microsoft.NET.Sdk.Razor, because this module ships .cshtml views that have to be compiled.
      A module with no views needs only Microsoft.NET.Sdk.
    -->
    <AssemblyName>DashboardModule</AssemblyName>
  </PropertyGroup>

</Project>
