<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- Clears the single-TFM default from samples/Directory.Build.props, which would win. -->
    <TargetFramework />
    <TargetFrameworks>net8.0;net10.0</TargetFrameworks>
    <AssemblyName>GreetingModule</AssemblyName>
  </PropertyGroup>

</Project>
