---
description: Inventory a .NET solution for a Modulith migration, without changing anything
---

Run Phase 0 of the `modulith` skill against $ARGUMENTS (or the current repository if no path is
given).

Read-only. Do not modify a single file except `modulith-assessment.md`.

Produce the assessment the skill's Assess reference describes — services, the call graph between
them, the data topology, deployment artefacts, the cross-cutting diff table, and the three kinds
of silent collision. End with the numbered batch of questions only the user can answer, and stop
there.
